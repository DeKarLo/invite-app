# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Optional: override DB name if you don’t want to use the one in URI
# DB_NAME=invite_db

# Render provides PORT automatically, don’t set it locally unless needed
# PORT=3000
